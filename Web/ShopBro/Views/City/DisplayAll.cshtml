@using FMASolutionsCore.Web.ShopBro
@model FMASolutionsCore.Web.ShopBro.ViewModels.CitiesViewModel
@{
    ViewData["Title"] = "Cities - View All";
    Layout = "~/Views/Shared/_LayoutShopBro.cshtml";
}
<div class="container rounded">            
    <div class="row">
        <div class="col-sm-12">
            <div class="text-left">
                <h3>City Details:</h3>            
            </div>        
        </div>
    </div>
</div>
<div class="container bg-light pt-2 pb-2">
    <form class="form-signin">
        @if (Model != null && Model.StatusMessage != null)
        {
            <Label class="error-text">@Model.StatusMessage </Label>
        }
        <div class="row">
            <div class="col-sm-12">
                <div class="text-left">                                    
                    <table class="table table-striped table-dark" id="DisplayAllTable">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Code</th>
                                <th>City Name</th>
                                <th>View</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Cities)
                            {
                                <tr>
                                    <td>@(item.CityID)</td>
                                    <td>@(item.CityCode)</td>
                                    <td>@(item.CityName)</td>
                                    <td><a href="/City/Search/@item.CityID" class="btn btn-outline-light btn-block">View</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>                
            </div>
        </div>
        <div class="col-sm-12 mt-5">
            <a href="/City" class="btn btn-outline-dark btn-block mb-3">Back</a>
        </div>            
    </form>
</div>

 
